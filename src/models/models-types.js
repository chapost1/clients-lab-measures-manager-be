const CLIENT = 'client'
const MEASURE = 'measure'
const SEX_TYPE = 'sex type'
const MEASURE_CATEGORY = 'measure category'
const MEASURE_VALUE_TYPE = 'measure value type'

module.exports = Object.freeze({
  CLIENT,
  MEASURE,
  SEX_TYPE,
  MEASURE_CATEGORY,
  MEASURE_VALUE_TYPE
})
